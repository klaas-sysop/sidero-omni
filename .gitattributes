# Ensure GPG key file uses LF line endings (not CRLF)
# This prevents "private key checksum failure" errors in Linux containers
*.asc text eol=lf

# Ensure shell scripts use LF line endings
*.sh text eol=lf

# Ensure YAML files use LF line endings
*.yml text eol=lf
*.yaml text eol=lf

